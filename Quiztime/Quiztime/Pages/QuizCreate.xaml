<Window x:Class="Quiztime.Pages.QuizCreate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quiztime.Pages"
        mc:Ignorable="d"
        Title="QuizCreate" Height="450" Width="800" Style="{StaticResource MainWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ItemsControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="9" Style="{DynamicResource MainGrid}"></ItemsControl>
        <Viewbox Grid.Column="2" Grid.Row="1">

            <TextBox x:Name="QuizTitleTxb" Margin="0,5,0,5" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" GotFocus="PlaceholderRemove" LostFocus="PlaceholderReturn" Text="Enter quiz title here" Foreground="Gray" ></TextBox>

        </Viewbox>
        <DataGrid x:Name="QuizGrid" Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="2" Grid.RowSpan="4" AutoGenerateColumns="False" CanUserAddRows="False" CanUserReorderColumns="False" Style="{DynamicResource QuizGrid}" CanUserSortColumns="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Picture">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Button Content="Add picture" Click="AddPicture" Tag="{Binding Id}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Question" Binding="{Binding Question}"></DataGridTextColumn>
                <DataGridTemplateColumn Header="Amount">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox SelectedIndex="3">
                                <ComboBoxItem Content="1"></ComboBoxItem>
                                <ComboBoxItem Content="2"></ComboBoxItem>
                                <ComboBoxItem Content="3"></ComboBoxItem>
                                <ComboBoxItem Content="4"></ComboBoxItem>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Answer 1" Binding="{Binding Answer1}"></DataGridTextColumn>
                <DataGridTemplateColumn Header="Correct">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <RadioButton Tag="1" GroupName="{Binding Id}" Checked="SetCorrectAnswer"></RadioButton>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Answer 2" Binding="{Binding Answer2}"></DataGridTextColumn>
                <DataGridTemplateColumn Header="Correct">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <RadioButton Tag="2" GroupName="{Binding Id}" Checked="SetCorrectAnswer"></RadioButton>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Answer 3" Binding="{Binding Answer3}"></DataGridTextColumn>
                <DataGridTemplateColumn Header="Correct">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <RadioButton Tag="3" GroupName="{Binding Id}" Checked="SetCorrectAnswer"></RadioButton>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Answer 4" Binding="{Binding Answer4}"></DataGridTextColumn>
                <DataGridTemplateColumn Header="Correct">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <RadioButton Tag="4" GroupName="{Binding Id}" Checked="SetCorrectAnswer"></RadioButton>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Column="1" Grid.Row="8" Content="Add Question" Click="AddQuestion" Style="{DynamicResource Button}"></Button>
        <Button Grid.Column="3" Grid.Row="8" Content="Save Quiz" Style="{DynamicResource Button}" Click="SaveQuiz"></Button>

    </Grid>
</Window>
